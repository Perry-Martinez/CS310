#include <iostream>

int main() {
    int var1, var2;
    double sum;
    
    std::cout << "Enter two numbers to add and multiply: ";
    cin >> var1 >> var2;
    
    sum = var1 + var2;
    
    std::cout << "Sum: " << result << std::endl;
    std::cout << "Product: " << var1 * var3 << std::endl;
    
}
